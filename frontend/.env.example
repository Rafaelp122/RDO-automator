# URL da API do backend (Cloud Run ou localhost)
VITE_API_URL=http://localhost:8000

# Chave de API compartilhada entre frontend e backend
# Deve ser igual ao valor de API_KEY no backend
# Em dev local, defina API_KEY=dev-key no backend também
VITE_API_KEY=dev-key
